# Copy this file to db.properties and fill in your real MySQL credentials.
# db.properties is git-ignored so secrets never reach the repository.

db.url=jdbc:mysql://localhost:3306/online_exam?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.user=YOUR_MYSQL_USER
db.password=YOUR_MYSQL_PASSWORD
